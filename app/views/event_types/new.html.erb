




<div class='container hero-unit'>
    <div class="row">
        <div class="col-md-10">
			<h1>New Event Type</h1>
        </div>

        <% if current_user && current_user.admin %>
            <div class="col-md-12">

                <%= render 'form' %>

            </div>

        <% else %>

            <div class="col-md-10">
                <p>You must be an administrator to create new event types</p>
            </div>

        <% end %>

    </div>
</div>