<div class="container">
	<div class="row">
		<div class="col-md-12">
			<h1>Events List</h1>
		</div>

		
	</div>
		<% @events.each do |event|%>
        	<%= render :partial => "events/event", :locals => {:event => event} %>
		<% end %>
	
	
</div>
